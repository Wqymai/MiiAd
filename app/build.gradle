apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    defaultConfig {
        applicationId "com.android.others"
        minSdkVersion 9
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('libs/GDTUnionSDK.4.9.544.min.jar')
}


task clearJar(type: Delete) {
    //删除以前有的
    delete 'libs/MGUnionSDK.jar'
}
task makeJar(type:org.gradle.api.tasks.bundling.Jar) {
    baseName 'MGUnionSDK07051013_TEST'
    //从哪里打包class文件,可以指定文件和目录
    from('build/intermediates/classes/release/com')
    //打包到jar后的目录结构
    into('com')
}
makeJar.dependsOn(clearJar, build)
